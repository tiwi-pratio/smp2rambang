[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["COREPACK_INTEGRITY_KEYS=\"\" corepack enable pnpm && COREPACK_INTEGRITY_KEYS=\"\" corepack prepare pnpm@9 --activate && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["BASE_PATH=/ NODE_ENV=production pnpm --filter @workspace/siakad run build && NODE_ENV=production pnpm --filter @workspace/api-server run build"]

[start]
cmd = "node --enable-source-maps artifacts/api-server/dist/index.mjs"
